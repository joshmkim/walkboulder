<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<style>
  body {
    background-color: #f8f9fa;
    font-family: sans-serif;
  }

  .profile-card {
    background-color: #fff;
    max-width: 500px;
    margin: 3rem auto; /* Added top margin */
    padding: 2rem;
    border: 1px solid #ccc;
  }

  .avatar {
    width: 100px;
    height: 100px;
    object-fit: cover;
    margin-bottom: 1rem;
  }

  h2 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .bio-text {
    font-size: 1rem;
    color: #555;
  }

  .custom-btn {
    display: inline-block;
    padding: 0.5rem 1rem;
    font-size: 1rem;
    background-color: black;
    color: white;
    border: 1px solid black;
    text-decoration: none;
    margin: 0 0.5rem;
    cursor: pointer;
  }

  .custom-btn:hover {
    background-color: #333;
  }

  .custom-btn.secondary {
    background-color: white;
    color: black;
  }

  .custom-btn.secondary:hover {
    background-color: #eee;
  }
</style>

</head>
<body>
  <div class="container text-center">
    <div class="profile-card">
      <!-- Avatar Image -->
      <img src="{{userData.avatar}}" alt="Profile picture" class="avatar mx-auto d-block" id="avatarPreview">

      <!-- User Name -->
      <h2>{{userData.name}}</h2>

      <!-- User Bio -->
      {{#if userData.bio}}
        <p class="bio-text">{{userData.bio}}</p>
      {{else}}
        <p class="bio-text text-muted">This user hasn’t written a bio yet.</p>
      {{/if}}

      <!-- Action Buttons -->
      <div class="mt-4">
        <a href="/settings" class="custom-btn">Edit Profile</a>
        <a href="/logout" class="custom-btn secondary">Logout</a>
      </div>
    </div>
  </div>
</body>
</html>
