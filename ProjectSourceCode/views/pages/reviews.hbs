<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reviews</title>
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Reviews</h1>
<button class="btn btn-primary mt-3" data-bs-toggle="modal" data-bs-target="#review_modal">
                Write Review
            </button> 

<div class ="modal" id="review_modal" tabindex="-1">
  <div class = "modal-dialog">
    <div class = "modal-content">
      <div class = "modal-header">
        <h5 class="modal-title">Write a Review</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
          <form id="review_form">
            <div class="mb-3">
                <label for="trail_name" class="form-label">Trail Name</label>
                
            </div>
            <div class="mb-3">
                        <label for="event_name" class="form-label">Rating</label>
                        <input type="number" id="rating" min="1" max="5" placeholder="1-5" required>
                        <input type="text" class="form-control" id="event_name" placeholder="share your thoughts on this trail" required>
                    </div>
                    
          
      </div>
              <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
              <button class="btn btn-primary" type="submit">
              Post
            </button>
            </div>
            </form>
    </div>
    

  </div>
</div>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="index.js"></script>

{{#if reviews.length}}
  <div class="reviews-list">
    {{#each reviews}}
      <div class="card mb-3">
        <div class="card-body">
          <h5 class="card-title">{{name}}</h5>
          <p class="card-text">Rating: {{rating}}/5</p>
          <p class="card-text">{{written_review}}</p>
        </div>
      </div>
    {{/each}}
  </div>
{{else}}
  <p>No reviews yet for this trail. Be the first to leave one!</p>
{{/if}}
</body>
</html>